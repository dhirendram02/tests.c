#include<stdio.h>
int main()
{
   int arr[]={7,1,5,4,9,10,3};
    int i,n,low;
    n=sizeof(arr)/sizeof(int);
    low=arr[0];
    for(i=1;i<n;i++)
    {
       if(low > arr[i])
       {
           low = arr[i];
       }
       
    }
           printf("%d\t",low); 

return 0;
} 
