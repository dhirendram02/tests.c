#include <stdio.h>

int main() {
	int arr[]={7,1,5,4,9,10,3};
	int n,sum=0,i,avg;
	n=sizeof(arr)/sizeof(int);
	for(i=0;i<n;i++)
	{
	    sum=sum+arr[i];
	}
	avg=sum/n;
	printf("%d",avg);
	return 0;
}

